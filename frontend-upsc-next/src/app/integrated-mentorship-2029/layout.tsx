import { metadataForPath } from "@/lib/seo/pages";
export const metadata = metadataForPath("/integrated-mentorship-2029");
export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
