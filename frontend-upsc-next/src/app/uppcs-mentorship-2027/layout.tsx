import { metadataForPath } from "@/lib/seo/pages";
export const metadata = metadataForPath("/uppcs-mentorship-2027");
export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
