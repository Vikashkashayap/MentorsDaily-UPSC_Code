import { metadataForPath } from "@/lib/seo/pages";
export const metadata = metadataForPath("/answer-evaluation");
export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
