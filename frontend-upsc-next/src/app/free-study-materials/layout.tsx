import { metadataForPath } from "@/lib/seo/pages";
export const metadata = metadataForPath("/free-study-materials");
export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
