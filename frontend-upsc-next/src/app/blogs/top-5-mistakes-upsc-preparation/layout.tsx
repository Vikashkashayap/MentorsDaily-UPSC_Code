import { metadataForBlogSlug } from "@/lib/seo/pages";
export const metadata = metadataForBlogSlug("top-5-mistakes-upsc-preparation");
export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
