import { metadataForPath } from "@/lib/seo/pages";
export const metadata = metadataForPath("/mains-pyqs");
export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
