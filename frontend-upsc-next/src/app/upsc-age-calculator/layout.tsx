import { metadataForPath } from "@/lib/seo/pages";
export const metadata = metadataForPath("/upsc-age-calculator");
export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
