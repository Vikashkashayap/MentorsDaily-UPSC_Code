# Copy to `.env` and adjust. Local dev: if `GET /api/v1/course/slug/:slug` returns 404
# because no Course document has that slug, map slug → MongoDB ObjectId of an existing course.
# Example (replace with your real course _id from MongoDB):
# COURSE_ID_BY_SLUG={"integrated-mentorship-2027":"673a1b2c3d4e5f6789012345"}
COURSE_ID_BY_SLUG=
